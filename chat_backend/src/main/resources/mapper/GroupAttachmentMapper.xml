<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.sandau.chat.dao.mapper.GroupAttachmentMapper">

    <select id="findGroupAttachmentByGroupId" parameterType="int" resultType="groupAttachment">
        select * from group_attachment where group_id = #{groupId}
    </select>

</mapper>
